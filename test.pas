Program test;

var
  a : char;
begin
  // le := 5;
  // // setLength(a,5);
  // // writeln(length(a));

  // // for i := 0 to le-1 do
  // // begin
  // //   a[i].c := i;
  // //   a[i].b := i+1;
  // //   writeln(a[i].c,a[i].b);
  // // end;
  // increment();
  // writeln(le);
  // test1.hehe();
  // test2.hehe();
  a := 'a';
  writeln(a);
end.